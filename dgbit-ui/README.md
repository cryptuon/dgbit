# dgbit-ui

Vue 3 + Tailwind single-page app that surfaces dgbit analytics, job state, and controls for running backtests/trading sessions.

## Quickstart
```bash
cd apps/dgbit-ui
pnpm install # or npm install / yarn
pnpm dev
```

Set `VITE_API_BASE_URL` in `.env` to point at `dgbit-api`.
